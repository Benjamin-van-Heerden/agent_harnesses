# Legal Agent Core

This directory contains the project-local legal harness runtime and durable practice state.

Managed runtime code lives under `.agent_core/harness/` and can be refreshed by `legal/setup.py`. Lawyer-owned practice state lives under `.agent_core/practice/`, `.agent_core/docs/legal_context.typ`, and top-level `clients/`.
